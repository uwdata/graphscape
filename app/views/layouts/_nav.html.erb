<nav class="navbar navbar-default">
  <div class="container">
    <!-- Brand and toggle get grouped for better mobile display -->
    <div class="navbar-header">
      <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#bs-example-navbar-collapse-1" aria-expanded="false">
        <span class="sr-only">Toggle navigation</span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
      </button>
      <%= link_to "VLRuler", root_path(), class: "navbar-brand" %>
    </div>

    <!-- Collect the nav links, forms, and other content for toggling -->
    <div class="collapse navbar-collapse" id="bs-example-navbar-collapse-1">

      <div class="navbar-form navbar-right" role="search">
        <div class="form-group">
          <div class="hidden" id="user-id" data-user-id="<%= @current_user.id %>" ></div>
              <input id="user-name" type="text" value="<%= @current_user.name || @current_user.id %>" class= "form-control input-sm" >
        </div>
        <input id="update-user-name" type="button" class= "btn btn-default btn-sm" value="Update Name"></input>
      </div>
      <ul class="nav navbar-nav navbar-right">
        <li><%= link_to "Triplet Matching", root_path() %></li>
        <li><%= link_to "Embedding", embedded_path() %></li>
        <li class="dropdown">
          <a href="#" class="dropdown-toggle" data-toggle="dropdown" role="button" aria-haspopup="true" aria-expanded="false">Rule <span class="caret"></span></a>
          <ul class="dropdown-menu">
            <li><%= link_to "List", rules_path() %></li>
            <li><%= link_to "Making", making_rules_path() %></li>
          </ul>
        </li>
      </ul>
    </div><!-- /.navbar-collapse -->
  </div><!-- /.container-fluid -->
</nav>